<?php include '../integrations/part/navbar.accueil.php'; ?>

<section id="middle-section">

    <div class="title-message">
    <i class="far fa-user icon-user"></i>
    <h1>Bienvenue sur votre page de connexion<br>
        "L'apprentissage est l'une des clés de la réussite"</h1>
    </div>

    <?php if (!empty($ERRORS)) : ?>
            <div class="error">
                <ul>
                <?php foreach ($ERRORS as $error) : ?>
                    <li><?= $error; ?></li>
                <?php endforeach; ?>
                </ul>
            </div>
        <?php endif; ?>

    <form id="form-connexion" action="connexion.php">
        <p>
            <input type="email" id="email" placeholder="Taper votre email" required>
        </p>
        <p>
            <input type="password" id="motdepasse" placeholder="Taper votre mot de passe" required>
        </p>
        <div>
        <button class="button button-success" type="submit">Connexion</button>
        </div>
    </form>

    
</section>
